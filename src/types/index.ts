export interface Product {
  productId: number;
  productName: string;
  unitPrice: number;
  quantity: number;
}

export interface Customer {
  customerId: number;
  customerName: string;
  email: string;
}